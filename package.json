{
	"name": "github-env-cleaner",
	"version": "1.0.4",
	"description": "Remove github deployment history",
	"main": "dist/index.mjs",
	"type": "module",
	"bin": {
		"github-env-cleaner": "dist/index.mjs"
	},
	"scripts": {
		"typecheck": "tsc --noEmit",
		"build": "pnpm typecheck && esbuild ./src/index.ts --outfile=dist/index.mjs && chmod +x dist/index.mjs",
		"prepare": "pnpm build"
	},
	"keywords": [],
	"author": "Mahdi Boomeri <mahdi.b2or@proton.me>",
	"license": "MIT",
	"devDependencies": {
		"@types/inquirer": "^9.0.1",
		"esbuild": "^0.15.7",
		"inquirer": "^9.1.1",
		"typescript": "^4.8.2"
	},
	"dependencies": {
		"octokit": "^2.0.7"
	}
}
